#include <stdlib.h>
#include <stdio.h>
#include <string.h>

char *ft_strtrim(const char *s1, const char *set)
{
    
}

int main()
{
    char string[] = "     Hello, world!     ";
    char set[] = " \t"
    printf("%s", ft_strtrim(string))
}